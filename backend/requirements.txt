# Backend do SaaS de NFS-e. Ver plano (Planejamento detalhado de construção
# > Stack técnico) para o racional de cada escolha.
sqlalchemy==2.0.54
psycopg[binary]==3.3.6
alembic==1.20.0
fastapi==0.141.1
uvicorn[standard]==0.46.0
# FastAPI usa isto pra form-data (upload do certificado A1 em
# /api/certificado) — em versões recentes do FastAPI não é mais uma
# dependência implícita, precisa declarar explicitamente.
python-multipart==0.0.20
pydantic==2.13.3
pydantic-settings==2.14.0
# Marco 10 (login): starlette.middleware.sessions.SessionMiddleware usa
# isto pra assinar o cookie de sessão — já vinha como dependência
# transitiva do fastapi, fixando explicitamente porque agora é requisito
# direto do login funcionar, não só um transitivo qualquer.
itsdangerous==2.2.0

# Biblioteca fiscal (app/fiscal/) — mesmas dependências de integracao/
# (ver integracao/requirements.txt), deliberadamente sem xmlsec/signxml.
lxml==6.1.0
cryptography==46.0.7
requests==2.33.1

# Marco 15 (item 4): cobrança/assinatura (ver app/services/billing.py). SDK
# oficial — usada só se STRIPE_SECRET_KEY estiver configurada (ver
# config.py); sem ela, os endpoints de cobrança respondem "não configurado"
# em vez de tentar falar com a API.
stripe==15.6.1

# Marco 15 (item 5): extração de texto de extrato bancário em PDF (ver
# app/services/extrato_pdf.py) — só leitura, nunca geração de PDF (isso
# continua sendo o skill de pdf do painel de documentos, não este backend).
pdfplumber==0.11.9

pytest==9.1.1
# Só usada em tests/test_extrato.py, pra gerar um PDF de extrato de
# exemplo na hora do teste (nenhum extrato real é versionado) — não é
# dependência de runtime do backend.
reportlab==4.4.10
